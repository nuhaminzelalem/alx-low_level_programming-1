#include "main.h"
/**
 * _islower - it checks for lowercase character
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
char lette;

if (lette > 'a' && lette < 'z')
{
	return (1);
}
else
{
	return (0);
}
}
