#include "main.h"
/**
 * add - adds two integres
 * @i: parameter
 * @j: the second operand to be added
 * Return: Always 0 (Succeed)
 */
int add(int i, int j)
{
	int sum;

	sum = i + j;
	return (sum);
}
